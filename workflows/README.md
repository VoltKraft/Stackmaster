# workflows/

Workflow definitions shipped with Stackmaster.

**Status:** not yet implemented.

- [`builtin/`](builtin/) — built-in workflow templates that operators
  **clone** into their own workflows. Ship as `*.example.yaml`, never
  hard-coded behavior.

See [../docs/WORKFLOWS.md](../docs/WORKFLOWS.md) and
[../docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md).
